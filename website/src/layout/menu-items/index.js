import dashboard from './dashboard';
import pages from './pages';
import pagesDashboard from './pages-dashboard';

// ==============================|| MENU ITEMS ||============================== //

export const clientItems = {
  items: [pages, dashboard]
};
export const dashboardItems = {
  items: [pagesDashboard]
};
